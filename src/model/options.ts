export enum OrderTabs {
  WaitingForPayment,
  WaitingForDelivery,
  BeReceived,
  CancelTheProduct,
}

export enum OrderStatus {
  WaitingForPayment,
  WaitingForDelivery,
  BeReceived,
  CancelTheProduct,
}

export enum RoleType {
  PERSONAL,
  COMPANY,
}

export enum PaymentStatus {
  SUCCESS,
  FAILED,
  PENDING,
  CANCELLED,
}

export enum BookingStatus {
  completed,
  inProgress,
  inPayment,
}

export enum CardSize {
  SMALL,
  MEDIUM,
  LARGE,
}

export enum AppColor {
  white,
  blue,
}

export enum ButtonVariant {
  text,
  contained,
  outlined,
}

export enum ButtonColor {
  primary,
  solid,
  disabled,
  secondary,
  text,
  white,
  error,
}

export enum ButtonSize {
  tiny,
  regular,
  small,
  mini,
}

export enum ButtonAlign {
  left,
  center,
  right,
  between,
}

export enum ButtonIconVariant {
  circle,
  box,
}

export enum ButtonIconColors {
  transparent,
  primary,
  solid,
  white,
  none,
}

export enum ButtonIconSize {
  small,
  medium,
  large,
}

export enum CarouselSize {
  small,
  medium,
  large,
}

export enum CatalogTitleColorActived {
  blue,
  red,
}

export enum ChipColor {
  default,
  primary,
  secondary,
  success,
  warning,
  error,
}

export enum InputVariant {
  outlined,
  filled,
  standard,
}

export enum InputSize {
  small,
  medium,
  large,
}

export enum InputSelectionSize {
  small,
  medium,
  large,
}

export enum InputSelectionVariant {
  outlined,
  filled,
  standard,
}

export enum LearnCardSize {
  small,
  medium,
  large,
}

export enum LearnCatalogMethod {
  learn,
}

export enum LinkSize {
  small,
  medium,
}

export enum LinkColor {
  gray,
  primary,
}

export enum ProductCatalogMethod {
  product,
  flashSale,
  solution,
  learn,
}

export enum InputSearchColor {
  white,
  gray,
  transparent,
}

export enum InputSearchVariant {
  outlined,
  filled,
  standard,
}

export enum FlashSaleRuntimeBgColor {
  red,
  white,
}
